---
phase: 11-docs-release-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - LICENSE
  - Cargo.toml
autonomous: true
requirements:
  - DOCS-01
  - DOCS-03
  - DOCS-05

must_haves:
  truths:
    - "README.md explains what AgenticCRM is, how to install it, and shows key usage examples for every CLI command"
    - "LICENSE file exists with MIT license text"
    - "cargo install --git works for end users (Cargo.toml has repository, license, readme metadata)"
  artifacts:
    - path: "README.md"
      provides: "Comprehensive project documentation"
      contains: "cargo install"
      min_lines: 150
    - path: "LICENSE"
      provides: "MIT license text"
      contains: "MIT License"
    - path: "Cargo.toml"
      provides: "Package metadata for cargo install"
      contains: "repository"
  key_links:
    - from: "README.md"
      to: "docs/mcp-setup.md"
      via: "markdown link"
      pattern: "docs/mcp-setup\\.md"
    - from: "README.md"
      to: "Cargo.toml"
      via: "install instructions match package name"
      pattern: "cargo install"
---

<objective>
Create the core public-facing documentation: a comprehensive README.md that replaces the outdated placeholder, a LICENSE file, and updated Cargo.toml metadata for cargo install support.

Purpose: The existing README.md is stale (references non-existent directories, calls the project "Rust-ready" when it IS a Rust CLI). The repo needs accurate documentation before public GitHub release.
Output: README.md (rewrite), LICENSE (new), Cargo.toml (metadata additions)
</objective>

<execution_context>
@/Users/pmannion/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pmannion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-docs-release-readiness/11-RESEARCH.md
@Cargo.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Cargo.toml metadata and create LICENSE</name>
  <files>Cargo.toml, LICENSE</files>
  <action>
1. Add metadata fields to Cargo.toml [package] section (after the existing `description` line):
   - `license = "MIT"`
   - `repository = "https://github.com/pmannion/agenticcrm"` (placeholder -- user can update)
   - `readme = "README.md"`
   - `keywords = ["crm", "contacts", "mcp", "cli", "carddav"]`
   - `categories = ["command-line-utilities"]`
   - `rust-version = "1.85"` (minimum for edition 2024)
   Do NOT change any existing fields or dependencies.

2. Create LICENSE file at repo root with standard MIT license text.
   - Copyright year: 2026
   - Copyright holder: "AgenticCRM Contributors"
   - Use the exact SPDX MIT license text from https://opensource.org/licenses/MIT
  </action>
  <verify>
    <automated>grep -q 'license = "MIT"' Cargo.toml && grep -q 'repository' Cargo.toml && grep -q 'keywords' Cargo.toml && test -f LICENSE && grep -q "MIT License" LICENSE && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Cargo.toml has license, repository, readme, keywords, categories, rust-version fields. LICENSE file exists with MIT text.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite README.md</name>
  <files>README.md</files>
  <action>
Completely rewrite README.md. The current content is outdated and must be replaced. Structure:

1. **Title and tagline**: `# AgenticCRM` with one-line description: "A plain-text, agent-friendly personal CRM. Contacts are markdown files with YAML frontmatter, managed through a Rust CLI, interactive TUI, or MCP server."

2. **Features section**: Bulleted list covering:
   - CLI with 15+ commands (add, list, search, show, edit, log, delete, archive, unarchive, due, bulk, bulk-update, sync, serve, tui)
   - Interactive TUI with contact browser, detail view, search, follow-up dashboard
   - MCP server for AI agent integration (stdio and Streamable HTTP transports)
   - iCloud/CardDAV bidirectional sync with conflict detection
   - Bulk operations with query engine (field predicates, dry-run, JSON pipe)
   - Plain-text storage (markdown + YAML frontmatter, git-friendly)
   - JSON output for all commands (`-f json`)

3. **Installation section**:
   - Prerequisites: Rust 1.85+ (`curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh`)
   - From GitHub: `cargo install --git https://github.com/pmannion/agenticcrm.git`
   - From source: `git clone`, `cargo build --release`, copy to PATH
   - Verify: `acrm --help`
   - Note: macOS is primary platform. Linux users need `libsecret`/`gnome-keyring` for sync features.

4. **Quick Start section**: 5-command flow:
   ```bash
   acrm add "Jane Smith"
   acrm edit "Jane Smith" --set company="Acme Corp" --set tags='["engineering","vip"]'
   acrm log "Jane Smith" -t coffee "Discussed Q3 roadmap"
   acrm search acme
   acrm due
   ```

5. **Usage section** with subsections for each command group. For each, show 1-2 example commands with expected behavior. Groups:
   - Contact Management (add, edit, show, list, search, delete, archive, unarchive)
   - Interaction Logging (log with types: coffee, call, email, message, conference, meeting, lunch, intro)
   - Follow-up Tracking (due)
   - Bulk Operations (bulk with query syntax, --set, --delete, --archive, --add-tag, --remove-tag, --dry-run, --yes; bulk-update --stdin with pipe example)
   - iCloud Sync (sync, sync pull, sync push, sync setup; mention --dry-run, --force, --tag, --status)
   - MCP Server (serve, serve --http --port 3000, serve --allow-sync; link to docs/mcp-setup.md for full guide)
   - Interactive TUI (tui)

6. **Contact Format section**: Show example contact file with frontmatter (first_name, last_name, email, phone, company, title, status, tags, etc.) and markdown body with interaction log.

7. **MCP Integration section**: Brief overview (2-3 sentences) explaining that `acrm serve` exposes all CRM operations as MCP tools. Show the simplest Claude Desktop config snippet (stdio). Link to `docs/mcp-setup.md` for full guide. List available MCP tools: search_contacts, show_contact, add_contact, edit_contact, log_interaction, delete_contact, archive_contact, due_follow_ups, sync.

8. **Configuration section**: Mention sync config at `~/.config/acrm/sync.toml`, set up via `acrm sync setup`.

9. **License section**: "MIT -- see [LICENSE](LICENSE)"

Do NOT include: team/contributor lists, badges/shields, changelog, CI status, screenshots (terminal output is fine as code blocks). Keep it factual and copy-paste-ready.

Cross-reference accuracy: Run `acrm --help` output (already captured: 15 subcommands) to ensure every command is documented. Do NOT document commands that don't exist.
  </action>
  <verify>
    <automated>test $(wc -l < README.md) -ge 150 && grep -q "cargo install" README.md && grep -q "acrm serve" README.md && grep -q "acrm sync" README.md && grep -q "acrm bulk" README.md && grep -q "acrm tui" README.md && grep -q "docs/mcp-setup.md" README.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>README.md is 150+ lines, covers all 15 CLI commands, includes installation instructions with cargo install, links to docs/mcp-setup.md for MCP guide, shows contact format example, and contains no references to outdated features (interactions/ directory, vdirsyncer, planned connectors).</done>
</task>

</tasks>

<verification>
- `grep -c "interactions/" README.md` returns 0 (no stale references)
- `grep -c "vdirsyncer" README.md` returns 0 (no stale references)
- `grep -c "Rust-ready" README.md` returns 0 (no stale references)
- `cargo check` passes (Cargo.toml changes valid)
- LICENSE file exists and contains "MIT License"
</verification>

<success_criteria>
- README.md accurately documents all 15 CLI subcommands with usage examples
- README.md installation section includes cargo install from git
- LICENSE file exists with MIT license
- Cargo.toml has license, repository, readme, keywords, categories metadata
- No references to outdated features (interactions/ dir, vdirsyncer, planned connectors)
</success_criteria>

<output>
After completion, create `.planning/phases/11-docs-release-readiness/11-01-SUMMARY.md`
</output>
