---
phase: 12-release-pipeline-with-cross-platform-builds-homebrew-chocolatey-and-install-script
plan: 01
subsystem: infra
tags: [cargo-dist, release-plz, git-cliff, homebrew, github-actions, ci-cd]

requires:
  - phase: 11-docs-and-release
    provides: "LICENSE, README.md, rust-version 1.85 in Cargo.toml"
provides:
  - "Cross-platform release workflow via cargo-dist (4 targets, 3 installers)"
  - "Automated version bumping and changelog via release-plz"
  - "Conventional-commit changelog generation via git-cliff"
  - "Platform-conditional keyring dependencies for cross-compilation"
affects: [12-02]

tech-stack:
  added: [cargo-dist 0.31.0, release-plz, git-cliff]
  patterns: [workspace.metadata.dist for cargo-dist config, PAT-based tag triggering]

key-files:
  created:
    - .github/workflows/release.yml
    - .github/workflows/release-plz.yml
    - release-plz.toml
    - cliff.toml
    - CHANGELOG.md
  modified:
    - Cargo.toml

key-decisions:
  - "[workspace.metadata.dist] not [dist] for cargo-dist 0.31.0 compatibility"
  - "RELEASE_PLZ_TOKEN (PAT) instead of GITHUB_TOKEN to enable tag-triggered downstream workflows"
  - "git_release_enable=false in release-plz (cargo-dist owns GitHub Releases)"
  - "publish=false in release-plz (not publishing to crates.io yet)"
  - "allow-dirty=['ci'] for cargo-dist workflow regeneration"

patterns-established:
  - "cargo-dist generate-ci for workflow generation (do not hand-edit release.yml)"
  - "Platform-conditional deps via [target.'cfg(target_os)'.dependencies]"

requirements-completed: [REL-01, REL-02, REL-03, REL-04, REL-05, REL-06, REL-07]

duration: 5min
completed: 2026-03-09
---

# Phase 12 Plan 01: Release Pipeline Configuration Summary

**cargo-dist cross-platform release pipeline with release-plz automation, git-cliff changelogs, and Homebrew tap publishing**

## Performance

- **Duration:** 5 min
- **Started:** 2026-03-09T21:59:57Z
- **Completed:** 2026-03-09T22:04:48Z
- **Tasks:** 2
- **Files modified:** 6

## Accomplishments
- Platform-conditional keyring deps for macOS/Linux/Windows cross-compilation
- cargo-dist release workflow generating binaries for 4 targets with shell, PowerShell, and Homebrew installers
- release-plz automation for version bumping, changelog generation, and tag creation on merge to main
- git-cliff conventional commit changelog configuration

## Task Commits

Each task was committed atomically:

1. **Task 1: Platform-conditional keyring and cargo-dist configuration** - `245826e` (feat)
2. **Task 2: Generate release workflow and create release-plz config** - `ae97e5a` (feat)

## Files Created/Modified
- `Cargo.toml` - Platform-conditional keyring deps, [workspace.metadata.dist] section, homepage field
- `.github/workflows/release.yml` - Generated by cargo-dist; tag-triggered cross-platform build and release
- `.github/workflows/release-plz.yml` - Automated version bump PR workflow on push to main
- `release-plz.toml` - release-plz configuration (changelog, tagging, no crates.io publish)
- `cliff.toml` - git-cliff conventional commit changelog template
- `CHANGELOG.md` - Initial changelog file for release-plz population

## Decisions Made
- Used `[workspace.metadata.dist]` instead of `[dist]` -- cargo-dist 0.31.0 requires workspace-level metadata
- RELEASE_PLZ_TOKEN (Personal Access Token) for release-plz workflow -- GITHUB_TOKEN-created tags do not trigger downstream workflows
- Set `git_release_enable = false` in release-plz -- cargo-dist handles GitHub Releases, not release-plz
- Set `publish = false` in release-plz -- not publishing to crates.io yet
- Added `homepage` field to Cargo.toml to suppress Homebrew formula warnings

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed [dist] section key to [workspace.metadata.dist]**
- **Found during:** Task 2 (workflow generation)
- **Issue:** Plan specified `[dist]` but cargo-dist 0.31.0 requires `[workspace.metadata.dist]`
- **Fix:** Changed section header to `[workspace.metadata.dist]`
- **Files modified:** Cargo.toml
- **Verification:** `cargo dist plan` succeeds, shows all 4 targets and 3 installers
- **Committed in:** ae97e5a (Task 2 commit)

**2. [Rule 2 - Missing Critical] Added homepage field for Homebrew formula**
- **Found during:** Task 2 (workflow generation)
- **Issue:** Homebrew publish job warned about missing homepage in Cargo.toml
- **Fix:** Added `homepage = "https://github.com/TheThoughtagen/agenticcrm"` to [package]
- **Files modified:** Cargo.toml
- **Verification:** `cargo dist plan` runs without warnings
- **Committed in:** ae97e5a (Task 2 commit)

---

**Total deviations:** 2 auto-fixed (1 bug, 1 missing critical)
**Impact on plan:** Both fixes required for cargo-dist compatibility. No scope creep.

## Issues Encountered
- `cargo dist` binary installed as `dist` not `cargo-dist` -- resolved by using full path `~/.cargo/bin/dist`
- `generate-ci` refused to run with `allow-dirty = ["ci"]` set -- temporarily removed, generated, then re-added

## User Setup Required

Before releases will work, the following GitHub repository setup is needed:
- Create a Personal Access Token (PAT) with `repo` scope
- Add it as repository secret named `RELEASE_PLZ_TOKEN`
- Create `TheThoughtagen/homebrew-tap` repository for Homebrew formula publishing

## Next Phase Readiness
- Release pipeline configured; 12-02 can add additional packaging or testing
- First release will be triggered when release-plz creates and merges a version bump PR

---
*Phase: 12-release-pipeline-with-cross-platform-builds-homebrew-chocolatey-and-install-script*
*Completed: 2026-03-09*
